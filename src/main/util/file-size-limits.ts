/** Storage admission is separate from whole-text UI and automatic indexing work. */
export const MAX_TEXT_FILE_BYTES = 200 * 1024 * 1024;
export const MAX_PROJECT_TEXT_PROCESSING_BYTES = 5 * 1024 * 1024;
